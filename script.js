const button=document.querySelector('.menu-button');const links=document.querySelector('.nav-links');if(button&&links){if(!links.querySelector('[data-account-link]')){const account=document.createElement('a');account.href='/account.html';account.textContent='Sign in';account.dataset.accountLink='true';links.appendChild(account)}button.addEventListener('click',()=>{const open=links.classList.toggle('open');button.setAttribute('aria-expanded',String(open));button.textContent=open?'Close':'Menu'});links.addEventListener('click',()=>{links.classList.remove('open');button.setAttribute('aria-expanded','false');button.textContent='Menu'})}
